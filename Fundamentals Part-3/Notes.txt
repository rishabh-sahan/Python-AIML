Strings
    It is a sequnce of characters
    Strings as a datatype is immutable in python

Lists
    It is a mutable sequence of values
    created using []
    It is ordered
    Lists Methods
        l.append(val) # add one element at the end
        l.insert(idx, val) # insert element at idx
        l.sort() # arranges in increasing order
        l.revrse() # reverses order

Tuples
    Immutable sequence of values
    created using ()

Dictionaries
    key:value pairs
    keys are unique
    It is Mutable and unordered

Sets
    Collection of unique elements
    It is Unoredered