NumPy -> Numerical Python
    designed for fast numerical computing
It is a fundamental package for scientific computing with python
    It has ndarray -> n dimensional array
python-list - flexible, slow
np arrays - homogenous, fast, optimized with C

Python Lists vs NumPy Arrays
    NumPy arrays are faster (Optimized in C)
    NumPy stores data in continuous memory blocks
    Uses less memory (optimized storage)
    Built-in mathematical functions
    Supports direct vectorized operations

Why is NumPy faster ?
    Homogeneous Data Types and Contiguous Memory
    Vector Operations & C Implementations
    Reduced Overhead

Oprations on NumPy Arrays
    Reshaping
    Indexing (1D & 2D array)
    Fancy & Boolean Indexing
    Slicing

Common NumPy Data Types
    Integers: int32, int64
    Floating point nums: float32, float 64
    Boolean: bool
    Complex nums: complex64, complex128
    String: S(byte-str) & U(unicode-str)
    Object: genric python objects - object

Axes in Multi-dimensional arrays

Vectorization & Broadcasting

Important Mathematical Functions
    Aggregation
        sum(), prod(), min(), max(), mean(), std(), var(), median(), argmin(), argmax()
    Power
        square(), sqrt(), pow(a, b)
    Log & Exponential
        log(), log10(), log2(), exp()
    Rounding()
        round(), floor(), ceil(), trunc()
    EXTRAS
        unique(), sort(), abs()

